version: '3.8'


services:
  back-end:
    build: 
      context: ./back-end
  front-end:
    build:
      context: ./front-end 
    depends_on:
      - back-end
  proxy:
    build:
      context: ./proxy
    ports:
      - "80:80"
    depends_on:
      - back-end
      - front-end
